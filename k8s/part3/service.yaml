apiVersion: v1
kind: Service
metadata:
  name: part3srv
  namespace: workshop
  labels:
    app: part3srv
spec:
  type: ClusterIP
  selector:
    app: part3srv
  ports:
    - name: http
      port: 8080
      targetPort: 8080
