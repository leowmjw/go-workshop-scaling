apiVersion: v1
kind: Service
metadata:
  name: part2srv
  namespace: workshop
  labels:
    app: part2srv
spec:
  type: ClusterIP
  selector:
    app: part2srv
  ports:
    - name: http
      port: 8080
      targetPort: 8080
