apiVersion: apps/v1
kind: Deployment
metadata:
  name: part2srv
  namespace: workshop
  labels:
    app: part2srv
spec:
  replicas: 1
  selector:
    matchLabels:
      app: part2srv
  template:
    metadata:
      labels:
        app: part2srv
    spec:
      containers:
        - name: part2srv
          image: workshop/part2srv:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
